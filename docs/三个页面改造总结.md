# 三个页面使用 TabsContainer 改造总结

## 已完成

✅ **TabsContainer 组件已更新**
- 已将全局的 tabs 样式搬入组件内部
- 组件位置：`project/src/components/common/TabsContainer.vue`
- 样式完全匹配原有的全局样式，不会有任何视觉变化

## 需要改造的三个页面

### 1. AboutPage.vue

**改动点：**

**Template 部分：**
```vue
<!-- 原来的 -->
<div class="tabs-wrapper">
  <div class="tabs">
    <div v-for="tab in tabs" ...>
      {{ tab.label }}
    </div>
  </div>
  <div v-if="currentTab === 'intro'">...</div>
  ...
</div>

<!-- 改为 -->
<TabsContainer :tabs="tabs" default-tab="intro">
  <template #default="{ currentTab }">
    <div v-if="currentTab === 'intro'">...</div>
    ...
  </template>
</TabsContainer>

<!-- 二维码弹窗保持在外部 -->
<div v-if="showQRCodes" class="qr-modal">...</div>
```

**Script 部分：**
```vue
<!-- 删除这些 -->
❌ import { useRouter, useRoute } from 'vue-router'
❌ import { computed } from 'vue'
❌ const router = useRouter()
❌ const route = useRoute()
❌ let currentTab = ref('intro')
❌ currentTab = computed(() => route.query.sub || 'intro')
❌ const changeTab = (tabName) => { ... }

<!-- 添加这个 -->
✅ import TabsContainer from '@/components/common/TabsContainer.vue'

<!-- 保留这些 -->
✅ const tabs = [...]
✅ const showQRCodes = ref(false)
✅ const projects = [...]
✅ function followClicked() { ... }
```

**Style 部分：**
- 保持不变，所有样式都保留

---

### 2. MapPage.vue

**改动点：**

**Template 部分：**
```vue
<!-- 原来的 -->
<div class="tabs-wrapper">
  <div class="tabs">
    <div v-for="tab in tabs" ...>{{ tab.label }}</div>

    <!-- 特征选择器在 tabs 内部 -->
    <div v-if="currentTab === 'map' && ..." class="feature-control-area">
      <SimpleSelectDropdown ... />
    </div>
  </div>

  <div class="tab-content">
    <MapLibre v-show="currentTab === 'map'" />
    ...
  </div>
</div>

<!-- 改为 -->
<TabsContainer :tabs="tabs" default-tab="map">
  <!-- 使用 tab-extra 插槽放置特征选择器 -->
  <template #tab-extra>
    <div v-if="currentTab === 'map' && ..." class="feature-control-area">
      <SimpleSelectDropdown ... />
      <HelpIcon ... />
    </div>
  </template>

  <!-- 主内容 -->
  <template #default="{ currentTab }">
    <div class="tab-content">
      <MapLibre v-show="currentTab === 'map'" />
      <DivideTab v-show="currentTab === 'divide'" />
      <CustomTab v-show="currentTab === 'custom'" />
      <CustomDataPanel v-if="currentTab === 'map'" />
    </div>
  </template>
</TabsContainer>
```

**Script 部分：**
```vue
<!-- 添加 -->
✅ import TabsContainer from '@/components/common/TabsContainer.vue'

<!-- 保留 -->
✅ const currentTab = computed(() => route.query.sub || 'map')
   // 注意：MapPage 需要保留 currentTab，因为 tab-extra 插槽中要用

<!-- 其他保持不变 -->
```

**Style 部分：**
- 保持不变

---

### 3. QueryPage.vue

**改动点：**

**Template 部分：**
```vue
<!-- 原来的 -->
<div class="tabs-wrapper">
  <div class="tabs">
    <div v-for="tab in tabs" ...>{{ tab.label }}</div>
  </div>

  <div class="tab-content">
    <div v-show="currentTab === 'tab1'">...</div>
    <div v-show="currentTab === 'tab2'">...</div>
    <div v-show="currentTab === 'tab3'">...</div>
    <div v-show="currentTab === 'tab4'">...</div>

    <!-- 共享组件 -->
    <LocationAndRegionInput ... />
    <button class="run-btn" ...>运行</button>
  </div>
</div>

<!-- 改为 -->
<TabsContainer :tabs="tabs" default-tab="tab2">
  <template #default="{ currentTab }">
    <div class="tab-content">
      <div v-show="currentTab === 'tab1'">...</div>
      <div v-show="currentTab === 'tab2'">...</div>
      <div v-show="currentTab === 'tab3'">...</div>
      <div v-show="currentTab === 'tab4'">...</div>

      <!-- 共享组件 -->
      <LocationAndRegionInput ... />
      <button class="run-btn" ...>运行</button>
    </div>
  </template>
</TabsContainer>

<FloatingDice ... />
```

**Script 部分：**
```vue
<!-- 添加 -->
✅ import TabsContainer from '@/components/common/TabsContainer.vue'

<!-- 删除 -->
❌ const router = useRouter()
❌ const route = useRoute()
❌ const currentTab = ref(route.query.sub || 'tab2')

<!-- 修改 watch -->
❌ watch(() => route.query, (newQuery) => {
     if (newQuery.tab === 'query' && newQuery.sub) {
       currentTab.value = newQuery.sub
     }
   })

<!-- 修改 onActivated -->
❌ onActivated(() => {
     if (route.query.tab === 'query' && !route.query.sub && currentTab.value) {
       router.replace({ query: { ...route.query, sub: currentTab.value } })
     }
     ...
   })

<!-- 其他保持不变 -->
```

**Style 部分：**
- 保持不变

---

## 改造后的优势

1. ✅ **代码更简洁**：每个页面减少 20-30 行代码
2. ✅ **逻辑统一**：tab 切换逻辑集中在 TabsContainer 中
3. ✅ **易于维护**：修改 tab 样式只需改一个地方
4. ✅ **样式不变**：完全保持原有的视觉效果
5. ✅ **功能不变**：路由同步、默认 tab 等功能完全一致

## 下一步

如果你同意这个改造方案，我可以：
1. 逐个改造这三个页面
2. 或者提供完整的改造后的文件内容供你复制

请告诉我你希望如何进行！
